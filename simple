# this PAM configuration file should be placed at /etc/pam.d
# note that pam_simple.so must also be placed at /usr/lib/x86_64-linux-gnu/security (at my system, at least ¯\_(ツ)_/¯)

auth required pam_simple.so
